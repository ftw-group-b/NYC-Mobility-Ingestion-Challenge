# Silver Source Queries

Each SQL file creates one trusted Silver table from Bronze:

- `01_green_taxi.sql`
- `02_taxi_zones.sql`
- `03_weather.sql`

Validation queries are maintained in `tests/silver/`.
