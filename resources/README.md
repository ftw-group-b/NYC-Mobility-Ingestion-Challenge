# Databricks Resources

`nyc_mobility_job.yml` defines the complete Databricks task graph from ingestion through the consolidated quality gate.

Source inspection is intentionally outside the scheduled job because it documents source structure rather than changing production tables.
