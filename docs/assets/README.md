# Documentation Assets

- `pipeline_architecture.svg` and `.png`: implemented Databricks task flow
- `star_schema.svg` and `.png`: implemented Gold fact and dimensions

The SVG files are the editable source. The PNG files are presentation-ready exports.
